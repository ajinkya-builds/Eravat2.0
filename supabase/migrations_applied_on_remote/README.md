# Migrations already applied on production

These SQL files were moved here so **local bootstrap** uses a single baseline snapshot:

- `supabase/migrations/20260220100000_remote_baseline_schema.sql`

They remain in the repo as history and match the remote migration list on project `mnytrlcmdpkfhrzrtesf`.

**Do not delete.** New schema changes should add **new** files under `supabase/migrations/` and be pushed to production per `docs/SUPABASE_OPERATIONS.md`.
