# Staging local env — copy to `.env.staging.local` (gitignored).
# Used by staging builds and certification scripts.

VITE_SUPABASE_URL=https://ttjtyvxfiqhjdngkgdkf.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<from Supabase dashboard → API → anon/publishable>
VITE_BASE_PATH=/
VITE_APP_ENV=staging
VITE_POSTHOG_PROJECT_TOKEN=<optional>
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Backend scripts (notification tests, seed) — staging project ONLY
SUPABASE_SERVICE_ROLE_KEY=<from Supabase dashboard → API → service_role secret>

# Push testing (optional — only if google-services.json is present)
# VITE_DISABLE_PUSH_NOTIFICATIONS=false

# Maestro / E2E
# E2E_BASE=http://127.0.0.1:4173
