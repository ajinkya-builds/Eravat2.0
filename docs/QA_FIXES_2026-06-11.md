# Eravat 2.0 — QA Fixes Applied 2026-06-11

**Scope:** H-1, H-5, M-6, M-7, L-3, L-6 from `QA_CODE_REVIEW_2026-06-09.md`.
**Verification:** `npm run build` clean (no chunk-size warning), `npx eslint .` clean (0 errors / 0 warnings), `npx tsc -b --noEmit` clean, **147/147 unit tests pass**, app smoke-tested in browser (boot, lazy routes, report form, admin conflict dashboard — no console errors).

---

## H-1 — AdminLive unscoped notification query ✅ (verified already fixed)
Found already resolved in working tree: `allQ` capped at `.limit(2000)`, notifications scoped to the
selected division via chunked `.in('report_id', …)` queries, unfiltered path capped at 5000
(`AdminLive.tsx`). No further change needed.

## H-5 — Bundle size ✅
- Route-level `React.lazy()` for all admin routes, map, report flow, profile pages (`App.tsx`), with a `<Suspense>` fallback.
- `manualChunks` vendor splitting in `vite.config.ts` (react / supabase / framer-motion / i18next / dexie).
- **Result:** main chunk 1,813 KB → 455 KB (519 → 128 KB gzip). Leaflet (277 KB) and Recharts (369 KB) now load only on map/chart pages. PWA service worker still precaches all chunks, so offline behaviour is unchanged.

## M-6 — Unguarded console calls ✅
- New `src/lib/logger.ts`: `log/info/warn` are dev-only; `error` always emits the static message but attaches detail objects (payloads, user IDs, error bodies) in dev builds only.
- All 54 `console.*` calls across 13 production files replaced. `grep` confirms zero unguarded calls outside `logger.ts`.

## M-7 — ESLint errors ✅ (119 errors / 17 warnings → 0 / 0)
- `eslint.config.js`: ignore generated dirs (`dev-dist`, `android`, `coverage`); test-scoped overrides (Playwright fixtures aren't React hooks; mocks may use `any`); context files exempt from `react-refresh/only-export-components`.
- Production `any`s replaced with structural types: new `src/types/adminRows.ts` for Supabase joined rows; `RegionAssignment` in AdminUsers; `errorMessage()` helper in `src/lib/errors.ts` replacing `catch (err: any)` patterns.
- Real fixes: `ProtectedRoute` timeout reset moved into effect cleanup; `AppSettings` memoization dep fixed (`user?.id` → `userId` variable); `AdminNotifications.load` wrapped in `useCallback`; `prefer-const`, `no-case-declarations`, unused vars, useless escapes cleaned; shared slider constants moved to `src/lib/radius.ts`.
- One stale test updated: `SyncService.extended.test.ts` asserted the removed L-3 retry ladder; replaced with pinned-`storage_path` assertions (+ PGRST204 now fails the report).

## L-3 — report_media column ladder ✅ (verified already fixed)
Found already resolved: `insertReportMedia` pins `storage_path` with a live-schema note. Added test coverage for the pinned payload.

## L-6 — Human Death / Injury / Grain Damage KPIs ✅
- **Migration** `supabase/migrations/20260611000000_add_conflict_damage_loss_categories.sql`: dynamically extends the `conflict_damages.category` enum with `grain`, `human_injury`, `human_death` (idempotent; no-op with a NOTICE if the column isn't an enum).
- **Data entry:** `LossType` union + `LOSS_TYPES` in the report form now include Grain Damage, Human Injury, Human Death; translations added in EN/HI/MR.
- **Sync:** `mapLossCategory` maps the new values to the new enum categories (previously everything unknown collapsed to `property`).
- **Dashboards:** AdminGeneral, AdminConflict, AdminLive now compute these KPIs from `conflict_loss_details` + `conflict_damages.category`; hardcoded `0` values, `⚠ Schema` badges, and schema-gap banners removed (replaced with a note that counts start from the feature's launch date). Pie chart colors added for the new categories.
- **Tests:** 6 new mapping cases in `SyncService.extended.test.ts`.

### ⚠️ Deploy order for L-6
Apply the migration **before** releasing this app build. If a report with a new loss type syncs before the enum values exist, the insert fails and the report stays in local retry (no data loss, but sync errors until the migration lands).

---

## Still open
| ID | Item | Status |
|----|------|--------|
| M-5 | `ADMIN_ROLES` vs `ROLE_HIERARCHY` divergence | Awaiting definitive admin-role list from PM |
| H-1 deep fix | Server-side notification aggregation RPC | Backlog (optional) |
